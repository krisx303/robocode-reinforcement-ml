package bot;

public class Categories {
    enum DistanceCategory { CLOSE, MEDIUM, FAR }
    enum AngleCategory { FRONT_TO_SHOT, FRONT, LEFT, RIGHT, BACK }
    enum SpeedCategory { STILL, SLOW, FAST }
    enum EnergyCategory { HIGH, MEDIUM, LOW }
}
