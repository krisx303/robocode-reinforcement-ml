package bot;

public class Categories {
    enum DistanceCategory { CLOSE, MEDIUM, FAR, UNKNOWN }
    enum AngleCategory { FRONT_TO_SHOT, FRONT, LEFT, RIGHT, BACK, UNKNOWN }
    enum SpeedCategory { STILL, SLOW, FAST }
    enum EnergyCategory { HIGH, MEDIUM, LOW }
}
